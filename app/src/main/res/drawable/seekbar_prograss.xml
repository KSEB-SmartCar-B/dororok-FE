<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp" />
            <solid android:color="@color/seekbar_background" />
        </shape>
    </item>
    <!-- Secondary progress (buffering) -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <solid android:color="@color/seekbar_secondary_progress" />
                <size android:height="2dp" /> <!-- Secondary progress의 높이 설정 -->
            </shape>
        </clip>
    </item>
    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <solid android:color="@color/seekbar_progress" />
                <size android:height="1dp" /> <!-- Progress의 높이 설정 -->
            </shape>
        </clip>
    </item>
</layer-list>
